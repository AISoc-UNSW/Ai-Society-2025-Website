# Discord bot dependencies
py-cord[voice]>=2.6.0

# Voice support dependencies
PyNaCl>=1.5.0

# HTTP client for backend communication
aiohttp>=3.9.1

# Audio processing
pydub>=0.25.1

# Added: openai for AI integration
openai>=0.27.0

# Added: google-generativeai for Gemini API
google-generativeai>=0.3.0

# Environment variable management
python-dotenv>=1.0.0

# PostgreSQL database adapter (using psycopg2-binary because psycopg2 gave a compilation error)
psycopg2-binary>=2.9.0

# SQL toolkit and Object-Relational Mapping (ORM)
sqlalchemy>=2.0.0

# Note: ffmpeg and opus library need to be installed separately on the system
# Windows: Download opus.dll and ffmpeg.exe, add to PATH
# Linux: sudo apt install libopus0 ffmpeg
# macOS: brew install opus ffmpeg